# Minimal requirements for Vercel serverless deployment
# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic[email]==2.5.0
python-dotenv==1.0.0
python-multipart==0.0.6
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
requests==2.31.0

# Database & External APIs
supabase==2.7.4
groq>=0.37.1
pytz==2023.3

# Lightweight ML (only what's actually used)
numpy==1.26.2
pandas==2.1.3
scikit-learn==1.3.2

# Note: Removed torch, transformers, voice services, and other heavy dependencies
# These are not needed for core habit tracking functionality
